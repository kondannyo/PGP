#!/usr/bin/env python

import os
from pathlib import Path
import re
import shutil
import unicodedata

WORK_DIR = Path(__file__).resolve().parent / 'Suttas'
INPUT_DIR = WORK_DIR / 'mahasalayatanika'
OUTPUT_DIR = INPUT_DIR / 'Output'
INDEX_PATH = WORK_DIR / 'RawIndex.tex'
PREP_DIR = INPUT_DIR / 'Prep'
